# Character Assets

Each playable character gets its own subfolder.

Current character:

- raider/ — assets for the Raider character.

Add a new character folder only when that character becomes playable, and include a README in that folder listing its exact PNG filenames.
