# Main Menu Button Assets

Upload the finished button art using these exact filenames:

- `play.png` — complete artwork for the **Play** button.
- `decks.png` — complete artwork for the **Build Decks** button.

Both images should use transparent backgrounds outside the button shape. The frontend scales them responsively and falls back to text buttons if either PNG is missing.
