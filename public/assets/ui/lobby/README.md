# Lobby Assets

Expected PNG:

- `background.png` — full-screen background artwork for the multiplayer lobby.

The frontend displays it with `background-size: cover`, centered across the viewport. If the PNG is missing, the lobby keeps the normal CSS background.
