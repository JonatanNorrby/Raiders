# Item UI Assets

Expected PNG:

- `holder.png` — decorative holder/platform shown behind the player's item controls in the lower-right corner.

The item buttons are positioned inside the holder with built-in padding. Transparent areas in the PNG are supported. If the image is missing, the item controls still work on a transparent background.
