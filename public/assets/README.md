# Assets

All production art belongs under this folder.

Current folders:

- cards/ — shared card frame plus unique card art.
- characters/ — character portraits and future character-specific art.
- ui/ — general interface and battlefield art.

Every asset subfolder contains its own README with the exact PNG filenames expected by the prototype. Missing PNGs are safe: the UI uses simple CSS fallbacks until art is added.
